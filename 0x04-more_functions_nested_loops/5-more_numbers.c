#include "main.h"
/**
 * more_numbers - Prints the numbers 0 to 14 ten times
 */
void more_numbers(void)
{
	int count, num;

	count = 0;
	num = 0;

	while
		(count < 10)
	{
		for
			(num = 0;
			 num <= 14;
			 num++)
		{
			_putchar(alph);
		}
		_putchar('\n');
		count++;
	}
}
