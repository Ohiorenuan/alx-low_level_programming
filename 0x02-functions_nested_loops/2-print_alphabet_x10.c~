#include "main.h"
/**
 *print_alphabet_x10 - prints 10 times the alphabet
 */


void print_alphabet_x10(void)
{
	char count = '0';
	char alph;

	while
		(count++ <= 57);
	{
		for
			(alph = 'a';
			 alph <= 'z';
			 alph++);

		_putchar(alph);
		_putchar(\n);
	}
}
